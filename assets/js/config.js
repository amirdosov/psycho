/* =========================================================================
   НАСТРОЙКИ  —  единственный файл, который нужно отредактировать
   ========================================================================= */

window.PSY_CONFIG = {

  /* Адрес приёмника результатов: веб-приложение Google Apps Script.
     Как его получить — пошагово написано в начале файла
     google-apps-script.gs. Должен оканчиваться на /exec           */
  gasUrl: 'https://script.google.com/macros/s/AKfycbwkZ1WyVdmVPTx7vuZKEHkVvgLb3MpvQoZwyb5_Oze7PJv2gFvIoKts3F48RGd6tQZu/exec',

  /* Спрашивать ли школу отдельным полем на стартовом экране */
  askSchool: false,

  /* Сколько раз молча повторить отправку при обрыве связи,
     прежде чем показать ученику кнопку «Отправить ещё раз» */
  retries: 2
};

/* -------------------------------------------------------------------------
   ПОЧЕМУ ЗДЕСЬ НЕТ НАСТРОЙКИ «ПОКАЗЫВАТЬ РЕЗУЛЬТАТ УЧЕНИКУ»

   Её нет намеренно. Ни ключи методики, ни подсчёт баллов, ни текст
   заключения на сайт не попадают вообще: браузер отправляет только
   выбранные варианты («в вопросе 7 отмечены а и д»), а считает, оформляет
   и рассылает всё Apps Script на стороне сервера. Поэтому результат
   нельзя ни увидеть на экране, ни скачать, ни вытащить из исходного кода
   страницы — его там просто нет.

   Адрес получателя тоже задан на сервере (переменная EMAIL в скрипте),
   а не здесь, — в открытом коде сайта его не видно.
   ------------------------------------------------------------------------- */
